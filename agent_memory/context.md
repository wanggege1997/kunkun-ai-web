# 项目上下文
- 仓库：`C:\Users\27887\kunkun-ai`
- 当前任务：按微信支付文档接入 PC 端 Native 扫码支付，前台不开放用户自助退款，保留管理员人工退款能力。
- 技术栈：Next.js App Router、Prisma、PostgreSQL、站内积分订单 `PaymentOrder`。
- 支付目标：下单返回微信 `code_url`，前端生成二维码；支付成功回调或查单兜底后幂等入账；超时订单可关单；退款仅管理员后台人工触发。
- 约束：最小范围改动，不回退用户已有的 `app/page.tsx` 改动；支付密钥和证书只通过环境变量或服务器证书路径配置，不写入仓库。
